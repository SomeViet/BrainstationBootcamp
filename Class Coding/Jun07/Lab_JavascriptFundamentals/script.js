//1. Write a statement that adds the numbers 3 and 5 and stores it in a variable.
// let statement = 3 + 5;
// console.log(statement);
// alert(statement);

//2. Create two string variables, one containing your first name and the other containing your last name. Concatenate them to display your full name.
// let firstName = "Kevin ";
// let lastName = "Tran";

// console.log(firstName + lastName);
// alert(firstName + lastName);

//3. Create two boolean variables (one true and one false). OR (||) and AND (&&) the variables together then inspect the result.
// let optionA = true;
// let optionB = false;

// console.log(optionA || optionB);
// alert(optionA || optionB);

// console.log(optionA && optionB);
// alert(optionA && optionB);
